import styles from "./Unauthorised.module.scss"

const Unauthorized = () =>{
  return(
    <div className={styles.unauthorised}>Sorry, you cannot access this page...</div>
  )
}

export default Unauthorized;